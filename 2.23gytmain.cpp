#include <iostream>

using namespace std;

int main()
{
    int x , y ,z ,q, w ;
    cout<<"请输入五个不同整数 ";
    cin>>x>>y>>z>>q>>w;
    if(x>y)
    if(x>z)
    if(x>q)
    if(x>w)
      cout<<"最大值为 "<<x;
    if(y>x)
    if(y>z)
    if(y>q)
    if(y>w)
      cout<<"最大值为 "<<y;
    if(z>x)
    if(z>y)
    if(z>q)
    if(z>w)
      cout<<"最大值为 "<<z;
    if(q>x)
    if(q>y)
    if(q>z)
    if(q>w)
      cout<<"最大值为 "<<q;
    if(w>x)
    if(w>y)
    if(w>z)
    if(w>q)
      cout<<"最大值为 "<<w;
    if(x<y)
    if(x<z)
    if(x<q)
    if(x<w)
      cout<<"最小值为 "<<x;
    if(y<x)
    if(y<z)
    if(y<q)
    if(y<w)
      cout<<"最小值为 "<<y;
    if(z<x)
    if(z<y)
    if(z<q)
    if(z<w)
      cout<<"最小值为 "<<z;
    if(q<x)
    if(q<y)
    if(q<z)
    if(q<w)
      cout<<"最小值为 "<<q;
    if(w<x)
    if(w<y)
    if(w<z)
    if(w<q)
      cout<<"最小值为 "<<w;
    return 0;
}
